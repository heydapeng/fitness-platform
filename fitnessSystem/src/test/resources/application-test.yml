spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

  flyway:
    enabled: true

  jpa:
    hibernate:
      ddl-auto: validate

  data:
    redis:
      repositories:
        enabled: false

logging:
  level:
    org.testcontainers: INFO